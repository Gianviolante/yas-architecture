// Figma node: 261:6871
// TODO: fetch singolo progetto da Sanity per slug
export default function ProgettoPage({ params }: { params: { slug: string } }) {
  return <div>Progetto: {params.slug} — WIP</div>;
}
